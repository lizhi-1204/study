server:
  port: 8080

spring:
  datasource:
    url: jdbc:mysql://sh-cynosdbmysql-grp-kmxoc9is.sql.tencentcdb.com:20982/study?useUnicode=true&characterEncoding=utf8&serverTimezone=Asia/Shanghai&useSSL=false
    username: root
    password: CtD9mb4u
    driver-class-name: com.mysql.cj.jdbc.Driver
  flyway:
    enabled: true
    locations: classpath:db/migration
    baseline-on-migrate: true
  jackson:
    time-zone: Asia/Shanghai

logging:
  level:
    root: INFO

app:
  jwt:
    secret: ${JWT_SECRET:change_me}
    expireSeconds: ${JWT_EXPIRE_SECONDS:7200}

